<IfModule alias_module>

    Alias /lightbox "CURPATH"
    <Directory "CURPATH">
        Options +Indexes
        AllowOverride All
        Require all granted
        ErrorDocument 403 /error/XAMPP_FORBIDDEN.html.var
    </Directory>

</IfModule>
